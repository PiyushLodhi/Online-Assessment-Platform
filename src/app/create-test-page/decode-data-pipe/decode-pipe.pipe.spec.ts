import { DecodePipePipe } from './decode-pipe.pipe';

describe('DecodePipePipe', () => {
  it('create an instance', () => {
    const pipe = new DecodePipePipe();
    expect(pipe).toBeTruthy();
  });
});
